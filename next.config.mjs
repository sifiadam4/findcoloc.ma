/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', "plus.unsplash.com", "media.wired.com", "lh3.googleusercontent.com","res.cloudinary.com"],
    },
};

export default nextConfig;
